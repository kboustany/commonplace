services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        DEV: "true"
    volumes:
      - .:/commonplace
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    env_file:
      - .env
    restart: unless-stopped
